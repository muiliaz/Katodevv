## Что меняется и зачем

<!-- Одним абзацем: какая была проблема и почему выбрано это решение.
     "Обновил зависимости" — не описание. "Обновил X, потому что Y" — описание. -->

## Как проверено

<!-- Не «прогнал тесты» — что именно доказано и чем.
     Если проверялось руками в браузере — так и написать, это нормально. -->

- [ ] `npm ci && npm run check:env && npm run lint && npm run check:dead && npm run test:coverage && npm run build`
- [ ] Проверено на deploy preview от Netlify

## Затронуто ли что-то из этого

- [ ] **Цены или срок ответа** — только через `src/shared/pricing.js`, значения меняет владелец
- [ ] **Защита форм** — honeypot, валидация, лимитер, Turnstile (см. `docs/adr/0002-*`)
- [ ] **Переменные окружения** — добавлены в `.env.example` и в панель Netlify
- [ ] **`/web`** — WebGL и GSAP тестами не покрыты, нужна проверка глазами на preview
- [ ] Ничего из перечисленного

## Что осталось незакрытым

<!-- Честно: частичное решение — нормально, необъявленное частичное решение — нет.
     Если что-то требует владельца или следующей сессии, напиши здесь. -->
