# 00 — План работ по 10 аудитам

**2026-08-05** · `muiliaz/Katodevv`, ветка `main` · аудиты проведены на `dc5dea2`

> Исторический документ: план на старте. Итог — в [`99-summary.md`](99-summary.md).

## Три оговорки к самим аудитам

**Аудиты проводились на форке.** Все 10 отчётов проверяли `anastasiakrivova-stack/-`; `git remote`
даёт `muiliaz/Katodevv`. Содержимое идентично, поэтому 9 отчётов применимы напрямую. Но
OSS-readiness 100/100 — **не достижение**: аудит увидел чистый форк, отказался оценивать и указал,
что предмет проверки — исходный репозиторий, то есть этот. OSS-готовность не измерялась ни разу.

**Продуктовый контекст есть, но не закоммичен.** Cognitive-debt ставит 0/10 за контекст для агентов.
Формально верно: в `git ls-files` 87 файлов, `PROJECT.md` среди них нет — он исключён `.gitignore`
как «Internal docs». Задача не сочинить с нуля, а извлечь и актуализировать.

**Находки пересекаются.** Отсутствие тестов на handlers штрафуется тремя аудитами, шаблонный README —
четырьмя. Одно исправление поднимает несколько оценок.

## Стартовая точка (замеренная)

| | Результат |
|---|---|
| Тесты | 15 passed |
| Сборка | успешно |
| `npm audit` | 51 (2 critical, 25 high) |

Из 51 прямых зависимостей с уязвимостями две: `react-scripts` и `react-router-dom`. Для ~20
транзитивных npm предлагает `react-scripts@0.0.0` — то есть фикса нет.

## Порядок и его обоснование

01 err.message → 02 тесты handlers → 03 лимитер → 04 CI-гейт → 05 зависимости → 06 мёртвый код →
07 конфигурация → 08 документация → 09 цены (разбор) → 99 итог

Одно отступление от приоритетов: **тесты (P1) раньше лимитера (P0)**. Шаг 03 меняет поведение
публичных handler-ов, которое не закреплено ничем; сначала сетка тестов, потом правка — иначе тесты
писались бы под уже изменённый код.

Документация предпоследней: написанная раньше, к концу работы она врала бы про удалённые файлы и
версии.

## Что решили не делать на старте

| Не делаем | Почему |
|---|---|
| Миграцию на Vite | Покрытие React-части 2%, e2e нет, ценность `/web` — WebGL и GSAP, которые сборка собирает молча. `main` автодеплоится в прод |
| Изменение цен и SLA | Верное значение из репозитория не выводится. Не угадывать |
| E2E, порог покрытия | Порог сейчас либо зафиксирует 2%, либо сразу покрасит CI |
| Prettier, разделение крупных модулей | Перепишет каждый файл / крупный рефакторинг без тестов UI |

**Забегая вперёд:** первые два пункта были сделаны позже — Vite на шаге 11, когда появились тесты
для проверки, цены на шаге 10, когда владелец назвал верные значения.
