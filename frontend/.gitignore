# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
# Playwright: the HTML report, and the traces/screenshots kept from failures.
/playwright-report
/test-results
# Browser binaries live outside the repo, but this is where Playwright caches
# its per-project state if a run is interrupted.
/.playwright

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
