# Реестр принятых предупреждений по зависимостям

Живой документ. Здесь фиксируются предупреждения `npm audit`, которые **осознанно оставлены
неисправленными**, с причиной, владельцем и датой пересмотра.

Смысл файла: без него нельзя отличить обдуманный технический долг от незамеченного дрейфа. Если
предупреждение не описано здесь — значит оно новое и требует разбора, а не молчаливого игнорирования.

**Последнее обновление:** 2026-08-08
**Состояние `npm audit` на эту дату:** **0 предупреждений**. Обоих принятых исключений больше нет —
таблицы ниже оставлены как история.

---

## Как пользоваться

```bash
cd frontend
npm audit                # текущее состояние
npm audit --omit=dev     # то, что реально попадает в runtime-установку
```

Если появилось предупреждение, которого нет в таблицах ниже — его нужно либо исправить, либо
добавить сюда с обоснованием. Пустая строка «принято» без причины не считается обоснованием.

---

## EXC-001 — Цепочка сборки `react-scripts@5.0.1` — ЗАКРЫТО 2026-08-07

**Было затронуто:** 16 записей — `@svgr/*`, `css-select`, `nth-check`, `postcss`, `svgo`,
`webpack-dev-server`, `workbox-*`, `serialize-javascript`, `rollup-plugin-terser` и другие.

**Статус:** исключение больше не нужно. `react-scripts` удалён из проекта — сборка переведена на
Vite, тесты на Vitest. Вместе с ним ушла вся цепочка: `npm audit` упал с 18 предупреждений до 2,
а lockfile — с 1384 записей до 229.

⚠️ Число 229 верно **только на коммите миграции** `de61d7a`. Дальше дерево росло вместе с
инструментами: ESLint → 325, knip и покрытие → 383, Playwright → **387** на 2026-08-08. Это не
регрессия — весь прирост в devDependencies, в runtime-установку он не попадает (`npm audit
--omit=dev` видит 82 prod-пакета из 386). Замер и таблица — в `audit-fixes/17-audit-rerun.md`.

Подробности решения и проверки — в `audit-fixes/11-vite-migration.md`.

Заодно стали не нужны и `overrides` для `underscore`, `yaml` и `@tootallnate/once`: эти пакеты
приходили только через `react-scripts`. Они удалены из `package.json`.

---

## EXC-002 — `react-router` GHSA-qwww-vcr4-c8h2 (RSC Mode CSRF Bypass) — ЗАКРЫТО 2026-08-08

**Закрыто без единой правки в проекте.** Версия как была `7.18.2`, так и осталась — обновилось само
предупреждение. GitHub изменил его **2026-08-07 в 18:16 UTC**, сузив уязвимый диапазон на ветке 7.x
с `>=7.12.0 <8.3.0` до `>=7.12.0 <7.18.2` и пометив 7.18.2 как первую исправленную версию. То есть
установленная у нас версия и есть исправленная.

Ниже — запись в том виде, в каком она была верна на 2026-08-07. Она полезна как урок: «исправленной
версии на ветке 7.x не существует» подтверждалось выводом `npm view` в момент написания и перестало
быть правдой через несколько часов. Реестр принятых предупреждений нужно перечитывать, а не
накапливать.

**Затронуто (2 записи):** `react-router@7.18.2`, `react-router-dom@7.18.2`
**Ссылка:** https://github.com/advisories/GHSA-qwww-vcr4-c8h2
**Уязвимый диапазон:** `>=7.12.0 <8.3.0`

**Статус:** ~~принято — неприменимо к этому проекту~~ → закрыто 2026-08-08, см. выше
**Владелец:** владелец репозитория
**Пересмотреть:** не требуется

### Почему не исправлено

Исправленной версии на ветке 7.x не существует, а ветки 8.x на момент записи ещё нет:

```
$ npm view react-router-dom dist-tags
{ "latest": "7.18.2", ... }     # 8.x не опубликована
```

Единственное, что предлагает npm, — откат на `7.11.0`. Это означало бы уйти на более старый выпуск
ради предупреждения, которое к проекту не относится.

### Почему это неприменимо

Предупреждение касается **режима RSC** (React Server Components) и обхода CSRF-защиты при выполнении
server actions. Этот проект — клиентский SPA:

- `frontend/src/app/App.jsx` использует `BrowserRouter`, `Routes`, `Route`;
- страницы используют `Link` и `useNavigate`;
- RSC, data routers, `action`/`loader` и серверный рендеринг не используются нигде.

Уязвимый код в бандл не попадает и исполняться не может.

### История

Исходный аудит гигиены зависимостей рекомендовал обновление `react-router-dom` 6.30.4 → 7.18.2, и
оно выполнено (шаг 05). Оно закрыло предупреждение, из-за которого рекомендация и возникла. Данное
предупреждение — **новое**, появившееся уже на рекомендованной версии. Это нормальная динамика, а не
ошибка обновления.

### План устранения

Обновиться до `react-router-dom` 8.3.0 или новее, когда она выйдет. Поверхность использования узкая
(пять API), поэтому обновление ожидается недорогим. Проверяется тестами в
`frontend/src/__tests__/routing.test.jsx`.

---

## Что было исправлено, а не принято (для истории)

| Дата | Что | Результат |
|---|---|---|
| 2026-08-05 | `npm audit fix` (без `--force`) | 51 → 31 предупреждение, обе critical закрыты |
| 2026-08-05 | `react-router-dom` 6.30.4 → 7.18.2 | закрыто исходное предупреждение по роутеру |
| 2026-08-05 | `overrides`: `underscore@^1.13.8`, `yaml@^1.10.3`, `@tootallnate/once@^2.0.1` | 31 → 18, все low закрыты |
| 2026-08-07 | Замена `react-scripts` на Vite + Vitest | 18 → 2, закрыт EXC-001 |
| 2026-08-08 | Ничего — GitHub сузил диапазон GHSA-qwww-vcr4-c8h2 | 2 → 0, закрыт EXC-002 |

Итого: **51 → 0**. Принятых исключений не осталось. Любое новое предупреждение — новое, и требует
разбора, а не молчаливого игнорирования.

